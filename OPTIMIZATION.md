# 0.2.0 预测检验修正版

本版本修复了会夸大或扭曲预测成绩的数据与计算问题，并建立后续观察口径。它没有证明下一期具有可重复的命中优势。

## 已修正
- 五个官方来源按各自 1/3/5/10 分钟周期计算下一期、跨日查询、漏期与覆盖率；日期及期数范围严格校验。
- 分析、走势图、选号和评分以有效公布号码为准。哈希推算保留作缺失字段兼容及核对，避免两套结果混用。
- 修复负概率，统一归一化；选号历史评估每一步只在该步之前的数据上重新拟合；所有1000个号码统一计分，选取数量不再改变同一个号码的分数。
- 新预测记录保存版本、依据期、生成时间、截止时间。号码及这些字段写入后不可覆盖；开奖数据更正时可以重新评分。
- 历史补算只补缺失并标为回放。旧记录保留为时间未验证档案。真实统计默认只包含可验证的开奖前记录；竞技场“全部档案”可查看旧数据。
- 每档实际号码数量决定随机基线、成本、ROI及滚动统计。Top3候选统一500注。
- AI可以回答证据不足、使用均匀权重；自评分不当成命中率。删除根据连输强制调整及根据单次命中确认假设的要求。
- 分析页“真实预测检验”提供固定配置的历史区间、Brier、对数损失和分箱对照。多个配置同一期不能当成独立样本。共享区块的数据源也不能合并为独立证据。
- 自动报告与方案维护只使用真实预测看板；回放视图不会把回放成绩反馈为真实表现。
- 本地后台自动记录默认300/500注配置并评分，不依赖打开分析页。

## 使用与升级
从系统托盘退出旧版，然后打开0.2.0。设置、加密密钥和原有数据使用原来的目录。
首次启动会自动添加审计字段；旧记录仍在，真实统计从能验证的记录开始累积。桌面“数据”菜单可以备份。
当前赔率仅为应用内模拟规则，统计中的金额也是模拟值。

## 如何观察
固定来源、策略、号码数量与配置，在预先约定的连续时间段观察全部结果。把命中率和同注数随机基线并排查看，同时检查样本量与区间。Brier和对数损失越低越好，但历史一次领先不等于可重复优势。分箱只是检查分数与实际频率是否相符，当前分数尚未校准。

## 当前边界
记录的版本、依据期和截止时间支持提前锁定核查，但不是完整输入快照；本版本不宣称历史回放完全还原当时的数据修订状态。没有调用真实付费模型来验证命中效果，也没有把短期领先认定为盈利证据。后续若研究新模型，应单独记录模型与参数版本、保留完整输入版本、预留时间验证集，再独立评估。

## 验证
自动回归覆盖迁移保留、不可变记录、开奖纠错、不同周期与跨日、历史前缀重生成、概率合法性、官方号码优先、基线与诊断指标、接口及本地模型路由。桌面使用隔离合成数据检查七个页面、指标呈现、托盘、备份和后台重启。
