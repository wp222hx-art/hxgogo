# 图谱工作台 · 0.3.1

独立入口：平台导航的图谱图标，或桌面菜单“工作台 → 图谱工作台”。地址为 /atlas。

## 数据自动同步更新

0.3.1：五个在线来源与原平台直接共用数据库，图谱每5秒核对持久版本。新增、更正、补漏和删除自动应用；编辑时保留输入，断线保留快照，恢复后继续追上。详见 [数据同步说明](docs/atlas-sync.md)。

## 本次截图分析

已逐张查看“彩票图片”文件夹全部77张PNG，分析索引为 docs/screenshot-review-a.json、b.json、c.json，应用资源为 public/static/atlas-screenshots.json。
其中085759_493仅6×7像素，无可辨识内容；085902_595与085919_955是完全相同的文件。不同期数、来源、弹窗或空态仍逐张保留。

截图展示的是一套“规则 → 候选集合 → 趋势与遗漏 → 方案观察”的工作流程。界面相似不代表有预测优势。本模块将筛选结果、历史描述和未来观察分开；不把排列分、遗漏值或K线形状称为命中概率。

## 对应关系

| 截图体系 | 图谱工作台中的实现 |
| --- | --- |
| 自选条件、代码条件、容错卡片 | 可搜索指标目录、每条选值/开出数/排除/数量容差、全局允许失败条数 |
| 结果列表、反集、分页、复制、导出 | 有界后台计算、完整计数、候选分页、补集、文本与数据导出 |
| 基本/平面/立体走势 | 数字、属性、位置、012路的可组合遗漏矩阵 |
| 和值、和合、差跨、均值、和振 | 统一指标及可核对计算定义 |
| 定位/排列、首尾、属性 | 固定数字位置或升序组合位置，保留原始开奖顺序供核对 |
| 11选5邻空、跳位、三码、差值排序 | 对应参数化指标与位置映射；原式不明的名称明确标注自定义口径 |
| 参考方案、添加K线、遗漏代码 | 保存独立工作区方案、显式号码集合、历史命中累计曲线 |
| 遗漏监测、遗漏数据、组合遗漏 | 双方案、起止排名区间、当前/最大遗漏和组合集合事件 |
| 胆拖、热胆 | 按明示包含事件统计，不用热胆包装预测概率 |
| 大乐透前区/后区 | 独立区域规则与观察；前5后2不会拼成同一号码域 |
| 智能模型/知识讲座 | 基于当前样本与公开计算口径的本地帮助；现有AI配置继续独立使用 |
| 足球菜单 | 独立赛事数据类型及历史比分视图；缺赔率、让球或半场字段时说明不可计算 |
| 格式设置/自定义来源 | 格式和公式参数保存、本地JSON导入、版本化共用API契约 |
| 篮球/双色球/手机版启动入口 | 截图只展示入口；保留扩展说明，不冒充已接通赛事、另一个彩种或手机安装程序 |

## 计算口径

- 和值为当前区域号码总和，合值为和值个位，跨度为最大值减最小值。三位和值区间为0–9/10–18/19–27；11选5任五为15–20/21–26/27–33/34–39/40–45。
- 两码差值、和值、和尾来自所有位置对。条件开出数保留重数，趋势每列每期只计一次。形态、单双和012个数按位置计数。
- 固定数字的位置保留原顺序；无序组合的定位按升序计算。三位观察从五位中投影时会显示投影名称，不更名为真实体彩来源。
- 任选N的趋势每期枚举所有N码子集，某列满足任一子集即该期命中，样本数仍按期，不能把子集数量当额外开奖样本。
- 和值振幅使用相邻记录之差；已知缺期时未知，不将缺期跨过去。无开奖日历的数据只声明相邻记录。
- 遗漏从当前观察窗口计数，窗口外历史未知；从未命中的数是下界。空数据不会产生伪造命中率。未知事件切断连续段。
- 定位遗漏300列表示≥300，不声称更长窗口内的精确值。
- 质合默认采用业务定义：1和数学质数归“质”，0和其余归“合”；可以调整1的归类。大小阈值在界面公开。
- 每条数量容差扩展允许开出数的上下界；全局容错表示允许失败的条件条数。空选条件不参与筛选；反集相对于当前候选全集。
- 排序分仅为历史位置/号码出现频次的平均值，不是经过校准的下一期命中概率。

### 原图没有公布的公式

A–E跳位位置映射、和值数据A/B、边邻、均衡、同差/同合分段、跳位分解、差值排序、部分三码组合、散度与偏度没有足够截图验证原公式。本模块为这些入口提供可见的计算定义或可配置映射，并标记“自定义口径”。不能声称已恢复原软件隐藏算法。

例如：边邻采用“与已选号码相邻的未选号码个数”；散度采用号码总体标准差取整；偏度采用标准化三阶中心矩。具体定义以指标目录为准。截图的定位分布还出现过球号与同排开奖号不一致，因此不复制其错误示例。

## 数据共用与扩展

新模块的旧来源适配器只读既有draws表，沿用现有后台同步，不会触发策略生成或AI请求。新导入数据和方案仅写atlas_records、atlas_workspace。原预测记录、配置、账户和算法不受影响。

所有图表来自同一快照，包含sourceId、schemaId、revision、真实号码数组、时间和质量状态。既有历史表没有首次观察时刻时observedAt为null。导入时observedAt为实际导入时间，不能反填为开奖前已经观察到的数据。

| 接口 | 用途 |
| --- | --- |
| GET /api/atlas/catalog | 规则、来源、能力和限制 |
| GET /api/atlas/snapshot?source=…&limit=300 | 从旧到新的同批数据，纯读取 |
| GET /api/atlas/status?source=… | 只读版本、最新期和后台状态 |
| POST /api/atlas/sync | 复用后台同步，无策略或AI调用 |
| POST /api/atlas/import | 校验本地数字/分区/赛事导入，单批最多3000条 |
| GET /api/atlas/workspace | 读取独立方案与参数 |
| PUT /api/atlas/workspace | 保存独立工作区，最多256KB |

版本为atlas.v1。未来同一供数API可按sourceId/schemaId映射到不同适配器，前端继续使用同一快照接口。没有自行猜测或安装未知彩票接口；现有五个来源之外的本地数据集要先导入数据。

导入检查号码个数、范围、重复、真实日期和期号。重复期号/赛事ID返回409，整批回滚，不静默覆盖旧记录。导出的未来供数数据可以通过这份契约进入相同分析机制。赛事目前为手动导入快照，不是联网比分直播。

截图中的奖金、过关或销售规则是历史文字，本模块没有将它们硬编码为现行规则。篮球、双色球等只见到启动菜单的入口有扩展记录；没有虚构其详情。

## 本地使用

退出旧版应用时从系统托盘选择“退出并停止后台”，再启动0.3.1。默认使用原数据目录，不需要重新填写AI配置。可以先在“数据”菜单备份。

内置五个来源共用已有数据。三位数字、11选5、大乐透、足球选择“本地导入”后，可在格式/API设置中查看样例和提交JSON。空数据集显示明确空态，仍可查看规则目录和生成合法候选空间。

新功能用于研究、历史描述和方案观察。是否能优于随机基线仍要由开奖前固化方案的未来独立样本验证，本次不宣称已经提高预测命中率。

## 验证记录

2026-09-06：53项自动回归通过，原网页构建通过。实际Electron验证原7个页面及图谱新页、条件筛选/保存、单次完整CSV导出、走势矩阵、任选子集、前后区合并、界面数据导入、双方案新到达记录监测、跨来源K线方案恢复、保险库、备份、托盘与重启持久化。测试使用独立目录与明确的合成记录，没有读取用户真实密钥或调用付费AI。

完整单区导出可输出全部候选；大乐透完整一注支持完整计数和分页，本次CSV可选择当前页或前10000条，范围会写明，不将局部文件称为全部结果。
