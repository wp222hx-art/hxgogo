# HashPlay Windows 本地工作台

## 使用

- 安装版：运行 `release/0.1.1/HashPlay-Setup-0.1.1.exe`，按提示选择安装目录。
- 免安装版：运行 `release/0.1.1/HashPlay-Portable-0.1.1.exe`。首次解压启动可能需要稍等。
- 也可直接运行 `release/0.1.1/win-unpacked/HashPlay.exe`，需保留同文件夹中的其他文件。
- 不需要额外安装 Node.js、数据库或 Cloudflare 工具。

启动后进入 **AI 配置中心**。顶部原生菜单可切换 AI 推荐、历史查询、量化分析、策略竞技场、优质策略和虚拟积分演示。

关闭窗口会隐藏到系统托盘，后台仍继续同步。通过托盘或“工作台 → 退出”彻底停止。关机和休眠期间不会持续运行，重新启动后按原有补齐机制恢复数据。

## AI 配置与本地调用

首次启动默认“停用 AI”。所有模型请求均由本机后台发送，页面不直接携带密钥访问模型服务。

- **云端 AI**：选择 DeepSeek 或 OpenAI 兼容，填写对应服务地址、密钥和模型，保存配置。OpenAI 兼容地址须包含相应版本路径，如服务要求的 `/v1`。
- **本机模型**：先自行启动提供 OpenAI 兼容接口的本机模型服务，再选择“本机模型”。在 `OPENAI_BASE_URL` 中填写本机地址（例如 `http://127.0.0.1:11434/v1`），在 `AI_MODEL` 中填写已安装模型的实际名称。该模式仅接受 localhost、127.0.0.1 或 ::1，不发送云端密钥。
- **校验**会实际调用所选服务。保存并启用后，后台可按数据节拍自动调用；云端服务可能计费。
- **停用 AI**暂停后续自动调用，保留已保存的设置。已发起的请求可能完成；数据同步和历史查询可继续运行。

本应用不附带模型权重，也不自动安装本机模型服务。实时数据及云端模型需要网络；历史数据和本地页面资源可在断网时保留。

## 数据、备份与密钥

默认数据目录为 Windows 当前用户的 `%APPDATA%/HashPlay Desktop`，以“数据 → 打开数据文件夹”显示的位置为准。

- `hashplay.sqlite`：历史数据、虚拟账户、策略结果、普通配置。
- `secrets.bin`：Windows 系统加密保护的 API 密钥。
- `desktop.log`：运行日志，超过限制后轮换。
- 浏览器账户状态保存在同一应用数据目录中，后台端口变化不会创建新账户。

使用“数据 → 备份数据”创建一致的 SQLite 备份。备份不包含 API 密钥。恢复时会先校验文件，并自动备份当前数据；恢复失败会尝试回滚。密钥保持不变，换电脑后需重新填写。卸载默认保留应用数据。

首次运行会新建数据库。GitHub 源码不包含原云端环境的历史数据库，本版本不会自动导入云端历史。

## 开发与构建

要求 Node.js 24 或更高版本，以及 npm。Windows 打包目标为 x64。

```powershell
npm ci
npm run desktop:test
npm run desktop:start
npm run desktop:dist
```

其他命令：

- `npm run desktop:build`：打包本地后台、数据库迁移和离线前端资源。
- `npm run desktop:smoke`：隐藏窗口执行桌面验证，数据保存在 `output/desktop-smoke`。
- `npm run desktop:pack`：仅生成可运行的目录。
- `npm run build`：保留原 Cloudflare 网页构建。

若 Electron 下载需要代理，使用环境中的 `HTTP_PROXY`、`HTTPS_PROXY` 和 `ELECTRON_GET_USE_PROXY=1`。这些只是构建环境设置，不会写入应用配置。

## 实现

桌面主进程管理窗口、托盘、系统加密和备份恢复；独立后台进程运行 Hono 和 SQLite。D1 适配层保留原有接口，并以事务执行批量语句。页面使用固定的私有应用地址，后台绑定本机随机端口并要求每次启动生成的访问凭证。

本地模型支持通过 Chat Completions 返回 JSON 的兼容接口。原有策略和模型生成效果未经此打包工作验证；桌面测试验证的是运行、存储、通信和界面功能。

当前构建未进行发行者代码签名。
